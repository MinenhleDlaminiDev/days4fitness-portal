NODE_ENV=development
PORT=5000

# Option 1: single URL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/days4fitness

# Option 2: separate values (used if DATABASE_URL is missing)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=days4fitness
DB_USER=postgres
DB_PASSWORD=postgres

